# SafeStep Navigator — Environment Variables
# Copy to .env and adjust as needed

# Demo settings (set these for judges)
VITE_USE_MOCK=true
VITE_DEMO_MODE=true

# Future: Backend API URL (when real backend is connected)
# VITE_API_URL=https://your-backend.example.com/api

# Future: Polygon RPC (when real blockchain is connected)
# VITE_POLYGON_RPC_URL=https://polygon-mumbai.g.alchemy.com/v2/YOUR_KEY
